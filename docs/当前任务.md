mobile项目，配置页

（以下已在代码中落实：`主页`、`#/config/main` 主将编辑、`#/config/deputy?slot=n` 副将编辑、`#/config?slot=n` 会重定向到副将页、`副将列表`10 槽战/休、`配置已认证` 与保存校验、文档 `04`/`10` 已同步。）

1、检测到未登录，或者自动登录失败，要跳转到登录页面，我发现有些场景没自动跳转

2，新增页面，“主页”，取代原来“配置”页面的位置，配置页面先别删，下面还有用
3，主页按当前数据情况显示：

主将数据，单独显示
| liyufei | 3转160级 | 男武-男武-男武-男武 | 速度型 |
| 3转160级 | 里飞沙 |

副将数据，每列都左对齐
| 南华仙人 | 3转160级 | 文文文文 | 速度型 | 战 |
| 南华仙人 | 3转160级 | 文文文文 | 速度型 | 战 |
| 南华仙人 | 3转160级 | 文文文文 | 速度型 | 战 |
| 南华仙人 | 3转160级 | 文文文文 | 速度型 | 休 |
| 无 |  |  |  |  |
| 无 |  |  |  |  |
| 无 |  |  |  |  |
| 无 |  |  |  |  |
| 无 |  |  |  |  |
| 无 |  |  |  |  |

主将名称和副将名称，使用看着就能点击的蓝色字体
点击主将名称，进入主将编辑页面（第2步删除的配置页面中的主将部分）
点击副将名称，进入副将编辑页面（第2步删除的配置页面中的副将部分）

副将有状态区分，战和休，战代表上阵，休代表休息，
最多上阵3个，上阵副将自动置顶
副将最多配置10个，没有配置的位置，名称显示无，也能点击，点击进入副将配置页面，

配置页面移除底部的三个按钮（保存，导入，导出）

在（主将数据，单独显示）的上方加一行，左侧靠左显示 已认证 或者 未认证，
右侧靠右显示，保存，导入json，导出json

前端点击保存后，后端校验数据是否符合要求，符合要求后，把该账号数据修改为“已认证”，并在前端显示，
如果未通过，显示未认证，（已认证状态下，做了一些改动，即使还原了，还是会显示未认证）










