目前唯一的效果就是亮边框和横幅显示和文本显示
core/types.ts 回合信息中，按顺序，每个事件渲染一次
每个事件刚开始渲染时，横幅显示事件对应的横幅显示，文本框追加显示事件对应的简要文本，文本框按事件加换行符

const sleep=速度×1时间隔2秒，速度×2时间隔1秒，速度×3时间隔0.5秒
如果速度选择的是 无动画，全部跳过，直接更新成最新状态，文本框直接填满文本

const 状态更新=根据新状态字段，变化的值都会体现在这里；根据buff字段，值是当前buff情况
同时震动一下方框

const a=战况事件中的主体，玩家名、武将下标
const b=战况事件中的目标，可能是一个可能是多个，也可能包含a（包含a时框的颜色以a为主）

关于亮框和横幅，要事件渲染完之后，才熄灭

接下来根据 动画名称 字段
# 普通攻击、暴击攻击、致命攻击、攻击未命中
# 舍命一击、力劈华山、排山倒海、固若金汤
# 画地为牢、趁火打劫、四面楚歌、金蝉脱壳、暗渡陈仓
# 呼风唤雨、妖火燎原、五雷轰顶、巫蛊极毒、毁天灭地
# 九转丹、龙涎露

a亮蓝框，b亮红框
sleep
状态更新
sleep

# 凌波微步

该事件，a还是事件主体，b是目标数组中的己方队伍，c是目标数组中的对方队伍
a亮蓝框，b亮红框
sleep
状态更新，此技能abc都存在状态更新，一起变化
sleep

# 防御

a亮蓝框
sleep

# 招将

直接把新副将渲染出来

# 无双开启、无双关闭

a亮蓝框
状态更新
sleep

# 毒发、爆炸、木牛流马

b亮红框
sleep
状态更新
sleep
