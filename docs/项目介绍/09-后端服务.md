# 09 · 后端服务（Node.js）

[← 返回索引](./README.md)

---

## 9.1 入口与配置

- **入口**：`nodejs/src/index.js`  
- **环境变量**：从 **仓库根目录** `.env` 加载（`dotenv` 在 `src/config/env.js` 中指向 `../../../.env`）。  
- **常用变量**：`SQLITE_PATH`（可选，默认仓库根目录 `data/huansan.sqlite`）、`JWT_SECRET`、`NODEJS_PORT`（或 `PORT`）、`NODE_ENV`。

---

## 9.2 中间件与横切

- **CORS**：`origin: true`，允许携带凭证。  
- **JSON body**：上限约 2MB（适配大配置）。  
- **限流**：`/api/` 下每分钟约 200 次（可按环境调整）。  
- **全局错误**：返回 `{ 错误: "服务器错误" }` 类 JSON（开发时控制台打印堆栈）。

---

## 9.3 数据模型 · 用户

**存储**：SQLite 单文件（默认 `data/huansan.sqlite`，扩展名 `.sqlite`），表 **`users`**。  
**连接**：`better-sqlite3`；启动时 `PRAGMA journal_mode=WAL`、`busy_timeout=8000`，便于 **Node 与 DB Browser / sqlite3 CLI 等同时连接**（写入仍会短暂互斥）。  
**仓储**：`nodejs/src/repositories/userRepo.js`

| 字段 | 说明 |
|------|------|
| `id` | 整数主键（JWT 内 `uid` 为其字符串） |
| `用户名` | 唯一，`COLLATE NOCASE` |
| `密码哈希` | bcrypt |
| `配置` | TEXT，存完整中文 key 配置 JSON |
| `配置已认证` | 0/1 |
| `创建时间` | ISO8601 文本 |
| `最近登录时间` | ISO8601 文本或空；**注册与登录成功** 时写入 |

---

## 9.4 路由模块（按前缀）

| 前缀 | 文件 | 说明 |
|------|------|------|
| `/api` | `routes/auth.js` | `POST /register`、`POST /login`（挂载在 `/api` 下，故完整路径为 `/api/register` 等） |
| `/api/config` | `routes/config.js` | JWT；读/写当前用户配置（读写前 `normalizeConfigDeep`） |
| `/api/attrs` | `routes/attrs.js` | JWT；返回计算后的属性 |
| `/api/admin` | `routes/admin.js` | **无 JWT**；用户列表分页、改密 |
| `/api/health` | `index.js` | 健康检查 |

---

## 9.5 Socket.IO

- 与 HTTP **同端口** 创建 `Server`。  
- V1：连接后推送欢迎事件；支持 `ping`/`pong`。  
- 后续可扩展：房间、用户绑定、PK 同步。

---

## 9.6 校验工具

**文件**：`nodejs/src/utils/validate.js`

- `validateCredentials`：注册/登录用户名+密码。  
- `validatePassword`：管理端改密、仅密码规则（长度与非法字符）。

---

## 9.7 相关文档

- [10-HTTP-API.md](./10-HTTP-API.md)  
- [13-环境与部署.md](./13-环境与部署.md)
