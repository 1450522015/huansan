# 04 · 配置数据模型（中文 JSON）

[← 返回索引](./README.md)

---

## 4.1 总结构

用户保存在 MongoDB 字段 **`配置`** 中的对象，根级包含四个键（均为中文）：

| 键 | 含义 |
|----|------|
| `主将` | 玩家主角色 + 装备 + 坐骑 |
| `副将1` | 副将槽位 1 |
| `副将2` | 副将槽位 2 |
| `副将3` | 副将槽位 3 |

**主将名称** 不要求写入配置：产品约定 **主将显示名 = 登录用户名**（由客户端展示）。

---

## 4.2 主将（`主将`）字段说明

| 字段 | 类型/结构 | 说明 |
|------|-----------|------|
| `角色分类` | 字符串 | `男武` / `男文` / `男异` / `女武` / `女文` / `女异`，**同时表达性别+职业**，不再单独存 `性别`、`职业` |
| `等级` | 数字 | 1–160 |
| `转数` | 数字 | 0–3；当前 UI 仅开放 **3 转** |
| `属性分配` | 对象 | `{ 体质, 智力, 力量, 敏捷 }`，规则见 [07-属性点与风格.md](./07-属性点与风格.md) |
| `装备` | 对象 | 六个部位，顺序见 [05](./05-装备坐骑宝石与技能表.md) |
| `坐骑` | 对象 | `{ 种类, 转数, 等级 }` |
| `天赋` | 数组（长度 4） | 每项 `{ 名称, 等级 }`，`名称` 可为 `null` |
| `技能` | 数组（长度 4） | 每项 `{ 名称, 等级, 熟练度 }`，名称由 `角色分类` **固定**，不可改槽位技能名 |

---

## 4.3 副将（`副将1`～`副将3`）字段说明

与主将相比：

- **无** `装备`、`坐骑`。  
- **有** `宝石`：数组，**最多 9 条**，每条 `{ 属性, 数值 }`，属性仅限九类抗性（见 [05](./05-装备坐骑宝石与技能表.md)）。  
- 其余：`角色分类`、`等级`、`转数`、`属性分配`、`天赋`、`技能` 含义同主将。

---

## 4.4 装备槽位（主将 `装备`）

每个部位一个对象：

```json
{
  "名称": "字符串",
  "词条": "部分部位需要，见装备表",
  "宝石": [ { "属性": "…", "数值": 15 }, null, null ]
}
```

- **`宝石`**：固定 **3 个槽位**，未镶嵌用 `null`。  
- **`数值`**：1–15（当前产品默认上限 15）。

---

## 4.5 规范化 `normalizeConfigDeep`

从数据库读出或客户端提交的配置，都会经 **`normalizeConfigDeep`**（定义在 [`data/gameCatalog.js`](../../data/gameCatalog.js)）处理后再保存或参与计算。主要行为：

1. **补全缺省**：与 `getDefaultConfig()` 合并，避免缺字段。  
2. **角色迁移**：若旧数据只有 `职业`+`性别`，会转换为 `角色分类`。  
3. **属性点修正**：`修正属性分配(等级, …)`，防止超过 `等级×8` 或低于每维 `等级`。  
4. **装备槽**：每部位走 `normalize装备槽`；旧键 **`项链`** 会迁移到 **`项饰`**。  
5. **天赋/技能**：补齐 4 槽；技能名与当前 `角色分类` 的固定表对齐。

---

## 4.6 相关文档

- [05-装备坐骑宝石与技能表.md](./05-装备坐骑宝石与技能表.md)  
- [07-属性点与风格.md](./07-属性点与风格.md)  
- [10-HTTP-API.md](./10-HTTP-API.md)（`/api/config`）
